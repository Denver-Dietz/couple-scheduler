1. Extract the `SelectedItemDrawer` component to its own file.
2. Update `DestinationsTab.jsx` and `DestinationsTabLeaflet.jsx` to import and use the new `SelectedItemDrawer` component.
3. Remove the duplicated rendering logic and helper functions (`handleAddLink`, `handlePromote`) from both tab components.
4. Verify functionality and build success.
